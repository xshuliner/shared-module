console.log('Hello, cli!');

console.log(process.env.NODE_DEV);

console.log(window.document.title);

var a = 10;

const xshuliner = 'xshuliner';

const yikan = 'yikan';

const fun = () => {
  console.log('', abc);
};
